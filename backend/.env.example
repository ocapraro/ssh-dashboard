# Environment Configuration for SSH Dashboard Backend

# Server Configuration
PORT=3001
NODE_ENV=development

# Log Directory Configuration  
LOG_DIR=/var/log/remote
MAX_LOG_LINES=1000
REFRESH_INTERVAL=30000

# CORS Configuration
FRONTEND_URL=http://localhost:3000

# API Rate Limiting (requests per 15 minutes)
RATE_LIMIT_MAX=100

# Logging Configuration
LOG_LEVEL=info
LOG_RETENTION_DAYS=30

# Security
API_KEY_REQUIRED=false
# API_KEY=your-secret-api-key-here

# Optional: Database Configuration (for future features)
# DATABASE_URL=mongodb://localhost:27017/ssh-dashboard
# REDIS_URL=redis://localhost:6379

# Optional: Email Notifications
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASS=your-app-password
# EMAIL_FROM=noreply@ssh-dashboard.com

# Optional: Webhook Notifications
# WEBHOOK_URL=https://hooks.slack.com/services/your/webhook/url
# WEBHOOK_EVENTS=error,device_offline,failed_login_threshold